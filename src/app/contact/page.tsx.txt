// --- File: src/app/contact/page.tsx ---
import React from 'react';
import type { Metadata } from 'next'; // Import Metadata type
import Button from '@/components/Button'; // Adjust path if needed
// Import necessary icons (assuming you have an Icons component or library)
// If not using a library, define SVG components like in previous examples
import { IconPhone, IconMail, IconClock, IconMap, IconBanknote, IconZap, IconCreditCard, IconPhoneCall, IconMessageSquareText } from '@/components/Icons'; // Example import path

// --- Constants (Define or import these) ---
const PHONE_NUMBER = "0492 992 211";
const EMAIL_ADDRESS = "Rb_automotive@hotmail.com"; // Consider a domain email
const BOOKING_IFRAME_URL = "https://calendar.google.com/calendar/appointments/schedules/AcZssZ0h7A71YAA1PX1pEBV00uwv3W8cn7R2eKCz-u2yPrLaieYqo-y8zA4RgVUtcqT1Hv1g2GZlY1HD?gv=true";
const BRAND_COLOR = 'red'; // Match Tailwind config

// --- Page-specific Metadata ---
export const metadata: Metadata = {
  title: "Contact RB Automotive | 24/7 Mobile Mechanic Perth",
  description: `Contact RB Automotive for 24/7 mobile mechanic services in Perth. Call ${PHONE_NUMBER}, email, or book online for diagnostics, repairs, servicing, and more.`,
  alternates: { // Optional: Define canonical URL for this page
    canonical: '/contact',
  },
  // Add Open Graph data specific to the contact page if desired
  // openGraph: {
  //   title: "Contact RB Automotive | 24/7 Mobile Mechanic Perth",
  //   description: `Get in touch with Perth's reliable mobile mechanic. Call ${PHONE_NUMBER} or book online.`,
  //   url: 'https://www.yourdomain.com.au/contact', // Replace with your actual domain
  //   images: [ ... ]
  // },
};


// --- Contact Page Component ---
export default function ContactPage() {
    return (
        <>
            {/* Metadata is handled by exporting the metadata object above */}

             {/* Reusing structure from ContactEnhanced section */}
             <section id="contact" className="py-16 lg:py-24 px-4 bg-gradient-to-b from-gray-100 to-gray-200 text-gray-800" aria-labelledby="contact-heading">
                <div className="container mx-auto">
                    <h1 id="contact-heading" className="text-3xl lg:text-4xl font-bold mb-12 text-center font-roboto-slab">Contact Us 24/7</h1>
                    <div className="grid grid-cols-1 md:grid-cols-2 gap-12 lg:gap-16 items-start">

                        {/* Contact Info Column */}
                        <div className="space-y-6 bg-white p-6 md:p-8 rounded-lg shadow-md border">
                            <h2 className="text-2xl font-semibold tracking-tight text-red-600 font-roboto-slab">Get In Touch Now</h2>
                            <p className="text-lg text-gray-700">
                                For emergency breakdowns, quotes, or booking enquiries, reach out anytime. We're here to help!
                            </p>
                            {/* Contact Details */}
                            <div className="space-y-4 text-gray-800 border-t pt-4">
                                 <a href={`tel:${PHONE_NUMBER}`} className="flex items-center space-x-3 group text-lg hover:text-red-700 transition-colors">
                                      <IconPhone className={`h-5 w-5 text-${BRAND_COLOR}-500 group-hover:text-${BRAND_COLOR}-700 flex-shrink-0`} />
                                     <span className="font-medium">{PHONE_NUMBER} (Call or Text)</span>
                                 </a>
                                 <a href={`mailto:${EMAIL_ADDRESS}`} className="flex items-center space-x-3 group text-base hover:text-red-700 transition-colors">
                                      <IconMail className={`h-5 w-5 text-${BRAND_COLOR}-500 group-hover:text-${BRAND_COLOR}-700 flex-shrink-0`} />
                                     <span className="">{EMAIL_ADDRESS}</span>
                                 </a>
                                 <div className="flex items-center space-x-3 text-base">
                                      <IconClock className={`h-5 w-5 text-${BRAND_COLOR}-500 flex-shrink-0`} />
                                     <span>Operating Hours: <strong>24 Hours / 7 Days</strong></span>
                                 </div>
                                 <div className="flex items-center space-x-3 text-base">
                                      <IconMap className={`h-5 w-5 text-${BRAND_COLOR}-500 flex-shrink-0`} />
                                     <span>Service Area: <strong>Perth Region & Beyond</strong></span>
                                 </div>
                            </div>

                            {/* Action Buttons */}
                             <div className="flex flex-col sm:flex-row gap-4 pt-4 border-t">
                                 <Button variant="primary" size="lg" asChild>
                                    <a href={`tel:${PHONE_NUMBER}`}> Call Us Now <IconPhoneCall /> </a>
                                 </Button>
                                  <Button variant="secondary" size="lg" asChild>
                                     <a href={`sms:${PHONE_NUMBER}`}> Send a Text <IconMessageSquareText /> </a>
                                  </Button>
                             </div>

                             {/* Payment Methods */}
                             <div className="pt-4 border-t">
                                <h3 className="text-xl font-semibold mb-3 tracking-tight text-red-600 font-roboto-slab">Payment Options</h3>
                                <div className="flex flex-wrap items-center gap-x-6 gap-y-3 text-gray-700 payment-methods">
                                    <div className="flex items-center text-sm"> <IconBanknote className="mr-2 text-green-500" /> Cash </div>
                                    <div className="flex items-center text-sm"> <IconZap className="mr-2 text-blue-500" /> Osko/PayID </div>
                                    <div className="flex items-center text-sm"> <IconCreditCard className="mr-2 text-purple-500" /> Credit/Debit Card </div>
                                </div>
                             </div>
                        </div>

                        {/* Booking Calendar Column */}
                        <div className="space-y-4 bg-white p-6 md:p-8 rounded-lg shadow-md border">
                            <h2 className="text-2xl font-semibold tracking-tight text-red-600 font-roboto-slab">Book Your Appointment Online</h2>
                            <p className="text-gray-700 text-sm">Select a time that suits you using our convenient online booking system.</p>
                            <div className="rounded-lg overflow-hidden shadow-inner border border-gray-300 bg-gray-50 h-[620px]">
                                <iframe
                                    title="Booking Calendar"
                                    src={BOOKING_IFRAME_URL}
                                    style={{ border: 0 }}
                                    width="100%"
                                    height="100%"
                                    frameBorder="0"
                                    loading="lazy"
                                ></iframe>
                            </div>
                            <p className="text-xs text-gray-500">*Online booking is for standard services. For emergencies, please call immediately.*</p>
                        </div>
                    </div>
                </div>
            </section>
        </>
    );
}
